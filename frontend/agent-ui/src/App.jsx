// App.jsx
// Root component — renders the chat window.
// Kept minimal on purpose — ChatWindow handles all the logic.

import ChatWindow from "./components/ChatWindow";

export default function App() {
  return <ChatWindow />;
}